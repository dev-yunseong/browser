# Handoff — 2026-04-24 17:xx

## Summary
Worked on priority issue `#126` (`[Layout] google.com — stray right-edge overflow artifact`). The fix landed in the layout engine by making auto-width floated boxes shrink-wrap correctly and then tighten to their actual laid-out child bounds. This removed the visible blue right-edge sliver from the clean headless `google.com` screenshot.

## Accomplished
- Claimed `#126` in `./.agents/PRIORITY.md` as in progress by `codex:gpt-5.4`
- Investigated the live Google render and confirmed the artifact was a clipped blue fragment at the far right edge
- Traced the issue to float sizing in `src/layout.rs`, not a generic raster/compositor bug
- Updated float sizing so auto-width floats use shrink-to-fit sizing instead of block fill-width behavior
- Added a follow-up width tightening step after child layout so floated containers reflect actual laid-out child bounds
- Added a regression test for a Google-header-like right-floated actions cluster
- Rebuilt and re-ran headless verification against `https://google.com` and `https://yunseong.dev`

## Current State
- Branch: `main`
- Modified files:
  - `src/layout.rs`
  - `./.agents/PRIORITY.md`
- Existing unrelated working tree changes still present and untouched:
  - `./.agents/handoffs/LATEST.md`
  - untracked historical handoff files
- `#126` is fixed locally based on clean headless CLI verification
- `#125` remains the next roadmap item after `#126`

## Code Changes
- `src/layout.rs`
  - introduced `is_floated`, `specified_width`, and `auto_width` handling in `perform_layout`
  - changed auto-width float sizing to use shrink-to-fit logic via intrinsic min/max content sizing
  - changed post-child sizing so floated auto-width boxes recompute width from actual laid-out content bounds
- `src/layout.rs` tests
  - added `test_float_right_auto_width_shrink_wraps_contents`

## Verification
- `cargo test -q test_float_right_auto_width_shrink_wraps_contents`
- `cargo test -q test_inline_zero_width_falls_back_to_intrinsic_for_positioned_children`
- `cargo build --bins`
- `drun --network host rust:latest timeout 30s ./target/debug/browser-cli --port 7070 navigate https://google.com`
- `drun --network host rust:latest timeout 30s ./target/debug/browser-cli --port 7070 navigate https://yunseong.dev`
- Clean headless screenshot check on `http://127.0.0.1:7070/screenshot`

## Key Decisions
- Treat this as a layout sizing bug, not a compositor-only bug.
  - Reason: the artifact tracked a partially offscreen Google header control, and element geometry already showed right-edge overflow.
- Use post-layout tightening for floated auto-width boxes in addition to intrinsic pre-layout sizing.
  - Reason: the initial intrinsic estimate still undercounted real child bounds in the Google header case.
- Verify with a fresh daemon after killing stale `rust:latest` containers.
  - Reason: an earlier screenshot was served from an old daemon on port `7070`, which gave misleading results.

## Next Steps
1. Mark `#126` done in `./.agents/PRIORITY.md` once you are ready to finalize it in the roadmap.
2. If desired, open/prepare the commit or PR for `#126` with the layout fix and regression test.
3. Move to `#125` (`[Layout] google.com — footer anchoring and link grouping`).
4. Separately investigate the broader Google top-right control/header fidelity, since the artifact is gone but that region is still not fully accurate.

## Open Questions / Risks
- The exact visual overflow fragment from `#126` is fixed, but the Google top-right controls still have broader fidelity issues not covered by this patch.
- `./.agents/PRIORITY.md` is still marked `in progress`; it was not flipped to done in this session.
- There are pre-existing warnings in the Rust build/test output unrelated to this issue.
